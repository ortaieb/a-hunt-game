PORT=3000
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins for cross-origin requests
# Use '*' to allow all origins (not recommended for production)
# Example: http://localhost:3001,https://admin.example.com
ALLOWED_ORIGINS=*